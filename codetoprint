#include <iostream>
#include <vector>
#include <set>
using namespace std;

int main() {
    int n, m;
    cin >> n;
    vector<string> arr1(n);
    for (int i = 0; i < n; i++) cin >> arr1[i];

    cin >> m;
    vector<string> arr2(m);
    for (int i = 0; i < m; i++) cin >> arr2[i];

    set<string> combinations; // ensures uniqueness

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            combinations.insert(arr1[i] + arr2[j]);
        }
    }

    // Print all unique combinations separated by a space
    for (auto it = combinations.begin(); it != combinations.end(); ++it) {
        if (it != combinations.begin()) cout << " ";
        cout << *it;
    }

    return 0;
}
