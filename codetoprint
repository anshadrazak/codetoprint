#include <iostream>
using namespace std;

struct Node {
    int element;
    int priority;
};

int main() {
    int n;
    cin >> n;

    Node arr[100];

    // Input
    for (int i = 0; i < n; i++) {
        cin >> arr[i].element >> arr[i].priority;
    }

    // Sort by priority (1 = highest priority)
    for (int i = 0; i < n - 1; i++) {
        for (int j = i + 1; j < n; j++) {
            if (arr[i].priority > arr[j].priority) {
                Node temp = arr[i];
                arr[i] = arr[j];
                arr[j] = temp;
            }
        }
    }

    // Print initial priority queue
    cout << "Priority Queue: ";
    for (int i = 0; i < n; i++) {
        cout << arr[i].element;
        if (i != n - 1) cout << " ";
    }
    cout << "\n";

    // Dequeue highest priority → remove first element
    for (int i = 1; i < n; i++) {
        arr[i - 1] = arr[i];
    }
    n--;

    // Print after dequeue
    cout << "Priority Queue: ";
    for (int i = 0; i < n; i++) {
        cout << arr[i].element;
        if (i != n - 1) cout << " ";
    }

    return 0;
}