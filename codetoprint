#include <iostream>
#include <string>
using namespace std;

int main() {
    string name;
    int age, updatedAge;

    getline(cin, name);
    cin >> age;
    cin >> updatedAge;

    cout << "Current age: " << age << endl;
    cout << "Updated age: " << updatedAge << endl;

    return 0;
}