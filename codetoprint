#include <iostream>
#include <vector>
#include <unordered_set>
using namespace std;

int main() {
    int n, m;
    cin >> n;
    vector<string> arr1(n);
    for (int i = 0; i < n; i++) cin >> arr1[i];
    
    cin >> m;
    vector<string> arr2(m);
    for (int i = 0; i < m; i++) cin >> arr2[i];
    
    unordered_set<string> seen;  // for uniqueness
    vector<string> result;       // preserve order
    
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            string combo = arr1[i] + arr2[j];
            if (seen.find(combo) == seen.end()) {
                seen.insert(combo);
                result.push_back(combo);
            }
        }
    }
    
    for (int i = 0; i < result.size(); i++) {
        if (i > 0) cout << " ";
        cout << result[i];
    }
    
    return 0;
}
