#include <iostream>
#include <limits.h>
using namespace std;

#define MAX_SIZE 10

int mainStack[MAX_SIZE];
int maxStack[MAX_SIZE];
int top = -1;
int max_top = -1;

void push(int ele) {
    if (top == MAX_SIZE - 1)
        return;

    top++;
    mainStack[top] = ele;

    if (max_top == -1)
        maxStack[++max_top] = ele;
    else
        maxStack[++max_top] = max(ele, maxStack[max_top - 1]);
}

void pop() {
    if (top == -1) {
        cout << "Stack is empty" << endl;
        return;
    }
    top--;
    max_top--;
}

int getMax() {
    if (max_top == -1)
        return -1;
    return maxStack[max_top];
}

void printstack(int arr[]) {
    if (top == -1) {
        cout << "Stack is empty" << endl;
        return;
    }
    for (int i = top; i >= 0; i--) {
        cout << arr[i];
        if (i != 0) cout << " ";
    }
    cout << endl;
}

int main() {
    int choice;
    while (cin >> choice) {
        switch (choice) {
            case 1: {
                int ele;
                cin >> ele;
                push(ele);
                break;
            }
            case 2:
                pop();
                break;

            case 3:
                cout << "Maximum element: " << getMax() << endl;
                break;

            case 4:
                printstack(mainStack);
                break;

            case 5:
                return 0;

            default:
                cout << "Invalid choice" << endl;
        }
    }
    return 0;
}