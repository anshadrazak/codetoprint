#include <iostream>
using namespace std;

int main() {
    int n1, n2;

    // Input size of Course A
    cin >> n1;
    int courseA[n1];
    for (int i = 0; i < n1; i++) {
        cin >> courseA[i];
    }

    // Input size of Course B
    cin >> n2;
    int courseB[n2];
    for (int i = 0; i < n2; i++) {
        cin >> courseB[i];
    }

    // Output: Course B first, then Course A
    for (int i = 0; i < n2; i++) {
        cout << courseB[i] << " ";
    }
    for (int i = 0; i < n1; i++) {
        cout << courseA[i] << " ";
    }

    return 0;
}
