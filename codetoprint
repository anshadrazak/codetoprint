Message* reverseInsertedMessages(Message* head) {
    if (head == nullptr) return nullptr;

    // The real list starts from head->next
    Message* first = head->next;

    return first;  
}
    }
    tail->next = newHead;  // make it circular again!

    return newHead;  
}