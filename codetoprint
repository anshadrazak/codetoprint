#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int n;
    cin >> n;
    for(int i = 1; i <= n; i++) {
        cout << fixed << setprecision(1) << setw(3) << 0.1 * i;
    }
    return 0;
}
