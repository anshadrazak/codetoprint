#include <iostream>
#include <vector>
#include <set>
using namespace std;

int main() {
    int n, m;
    cin >> n;
    vector<string> arr1(n);
    for (int i = 0; i < n; i++)
        cin >> arr1[i];

    cin >> m;
    vector<string> arr2(m);
    for (int i = 0; i < m; i++)
        cin >> arr2[i];

    set<string> uniqueCombinations;

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            uniqueCombinations.insert(arr1[i] + arr2[j]);
        }
    }

    for (auto it = uniqueCombinations.begin(); it != uniqueCombinations.end(); ++it) {
        cout << *it;
        if (next(it) != uniqueCombinations.end())
            cout << " ";
    }

    return 0;
}
