#include <iostream>
using namespace std;

int front = 0, rear = 0;

int main() {
    int N;
    cin >> N;                 // number of elements

    int *queue = new int[N];  // array-based queue

    // Enqueue N integers
    for (int i = 0; i < N; i++) {
        int x;
        cin >> x;
        queue[rear++] = x;
    }

    // Footer snippet (already given in your screen)
    cout << "Dequeuing elements: ";
    while (front < rear) {
        cout << queue[front++] << " ";
    }
    cout << endl;

    delete[] queue;
    return 0;
}